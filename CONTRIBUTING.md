# CONTRIBUTING — git 記載規約

このリポジトリで Issue / branch / commit / PR / レビューコメント / release に書く内容の規約です。
目的は **レビュアーが「コード」と「完成保証の証跡」だけを確認すれば済む状態を作る** ことです。
文章の上手さは求めません。決まった場所に決まった形で書いてください。

---

## 0. 全体の流れ

```
Issue 起票 ──▶ branch 作成 ──▶ commit ──▶ PR 作成 ──▶ レビュー ──▶ squash merge ──▶ release
  §2            §3             §4         §5          §6           §7              §8
```

- 1 Issue = 1 branch = 1 PR。複数 Issue を 1 PR にまとめない
- PR は Issue の「完了条件」を満たしたことを証明する場所。理由や背景は Issue に書き、PR には書かない
- **診断の原則**：diff を見れば分かることは書かない。diff を見ても分からないこと（意図・確認方法・見どころ）だけ書く

---

## 1. 共通ルール

| ルール | 内容 |
|---|---|
| 分量 | 1 項目 = 1 画面以内（目安 400〜800 字）。超えるなら分割する |
| 自由記述 | 各項目 1 ブロックのみ、200 字以内 |
| 箇条書き | 原則 3 点以内。5 点を超えるなら Issue / PR を分ける |
| 証跡 | 「テストした」「確認した」という**文章は禁止**。コマンド＋結果、または CI の URL を貼る |
| 言語 | 本文は日本語。branch 名・commit の type・scope は英語 |

---

## 2. Issue

Issue はテンプレート（Issue Forms）から起票します。空白 Issue は作成できません。

### タイプ

| 接頭辞 | 用途 | PR 紐付け |
|---|---|---|
| `[Bug]` | 期待と異なる動作 | あり |
| `[改修]` | 既存機能の変更・改善・リファクタ | あり |
| `[作業]` | コード変更を伴わない作業（設定・調査・文書） | 任意 |
| `[アイデア]` | 提案・検討事項。実装前提ではない | **なし**（着手時は `[改修]` を起票し直す） |

### 書き方

- タイトル：`[タイプ] 対象 + 何が/どうなる`、40 字以内。原因の推測をタイトルに書かない
- **完了条件は必須**。チェックボックス形式で、第三者が確認できる状態を書く
  - NG：`- [ ] 修正する`
  - OK：`- [ ] 手順 3 で一覧が表示される` / `- [ ] 回帰テスト xxx を追加し CI で通過`
- 背景・課題は 300 字以内。変更内容は 3 点以内
- 「対象外」に、この Issue で扱わないことを明記する（後続 Issue があれば番号を書く）

---

## 3. branch

```
<type>/<issue番号>-<英語の短い説明>
```

| type | 用途 |
|---|---|
| `feat` | 機能追加・仕様変更 |
| `fix` | 不具合修正 |
| `refactor` | 挙動を変えないコード整理 |
| `docs` | ドキュメントのみ |
| `test` | テストのみ |
| `chore` | 設定・依存更新・雑務 |

- 例：`fix/123-login-timeout`、`feat/456-export-csv`
- 説明部分は小文字・ハイフン区切り・3 語以内
- 自由記述は入れない。Issue 番号のない branch は作らない

---

## 4. commit

Conventional Commits 準拠。**squash merge を採用しているため、commit 単位の粒度は問いません**が、レビュー中に commit を追えるよう形式だけは守ってください。

```
<type>(<scope>): <要約> (#<issue番号>)

<本文：任意、3 行以内>
```

- 件名は 50 字以内。文末に句点を付けない
- `type` は §3 と同じ
- `scope` は変更対象（例：`api`, `auth`, `ui`, `db`）。省略可
- 本文は「なぜこの commit が必要か」が diff から読めない場合のみ書く
- 例：`fix(auth): セッション更新時のタイムアウト判定を修正 (#123)`

---

## 5. Pull Request

PR テンプレートが自動挿入されます。**見出しを消さず、順番を変えず、全項目を埋めてください。**

### PR タイトル

squash merge 後にそのまま履歴になります。commit 件名と同じ形式で書きます。

```
<type>(<scope>): <要約> (#<issue番号>)
```

### 各項目の書き方

| 項目 | 書き方 |
|---|---|
| 対応 Issue | `Closes #123`。1 つだけ |
| 変更概要 | 何を変えたか、3 点以内。理由は書かない（Issue にある） |
| 完了条件の充足 | Issue の完了条件を**そのまま転記**し、満たしたものに ✓ |
| 検証証跡 | CI の URL、または実行コマンド＋結果の貼付。**空欄・文章のみは差し戻し** |
| レビュー観点 | 特に見てほしい箇所を `ファイル名:行` または関数名で、3 点以内 |
| 補足 | 迷った判断・既知の制約・後続 Issue。200 字以内・任意 |

### Draft の使い方

- 完了条件に未 ✓ がある → Draft で出す
- 検証証跡が埋まっていない → Draft で出す
- Draft はレビュー対象外。「Ready for review」にした時点でレビュー依頼とみなす

---

## 6. レビューコメント

1 コメント 1 論点、3 文以内。先頭に必ず重要度を付けます。

| 接頭辞 | 意味 | 対応 |
|---|---|---|
| `[must]` | 修正しないとマージしない | 必ず対応 |
| `[should]` | 対応推奨。しない場合は理由を返す | 対応 or 理由を返信 |
| `[nits]` | 些細な指摘・好み | 任意 |
| `[ask]` | 質問。修正要求ではない | 回答する |

- 指摘には「何が問題か」と「どうすればよいか」の両方を書く。「これは違う」だけは禁止
- レビュアーは §5 の項目が埋まっていない PR を**読まずに** Draft へ戻してよい
- 修正後は対応した commit または「対応済み」を返信し、コメントの解決は**レビュアー**が行う

---

## 7. merge

- **squash merge のみ**。merge commit / rebase merge は使わない
- squash 時のメッセージは PR タイトルをそのまま使う（既定で入る）
- マージ条件：`[must]` がすべて解決済み、必要な承認数を満たす、必須チェックが通過
- マージは PR 作成者が行う（レビュアーは承認まで）

---

## 8. release

- リリースノートは PR タイトルから自動生成する。人間が書くのは**ハイライト 3 行まで**
- ハイライトは「利用者に見える変化」だけ書く。内部リファクタは書かない
- タグは `vX.Y.Z`（Semantic Versioning）

---

## 9. 差し戻し基準（まとめ）

以下に該当する PR は、内容を読まずに Draft へ戻します。個人への評価ではなく、規約の運用です。

1. PR テンプレートの見出しが欠けている・順番が違う
2. 「検証証跡」が空、または文章のみ
3. 「完了条件の充足」が Issue と一致していない
4. 対応 Issue がない、または複数 Issue を 1 PR にまとめている
5. branch 名・PR タイトルが §3 / §5 の形式に合っていない

---

## 10. 参考：ローカルで自動チェックしたい人向け（任意）

規約の守り方は各自に任せます。以下は手段の一例で、導入は必須ではありません。

- **commit 形式**：[commitlint](https://commitlint.js.org/) + [husky](https://typicode.github.io/husky/) で `commit-msg` フックを設定
- **branch 名**：`git config` の alias で `git start <type> <issue> <desc>` のようなコマンドを作る
- **PR 作成**：`gh pr create --template` でテンプレートを確実に読み込む

---

## 改訂

この規約は運用しながら見直します。変更提案は `[アイデア]` Issue で起票してください。
